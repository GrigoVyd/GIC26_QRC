# GIC 2026 Quantum Reservoir Computing Challenge
# Core quantum simulation
qiskit>=1.2.4,<2.2
qiskit-aer>=0.14.0

# Numerical / ML stack
numpy>=2.0,<2.5
scipy>=1.13.0
scikit-learn>=1.5.0
pandas>=2.2.0

# Visualization
matplotlib>=3.9.0
seaborn>=0.13.0

# Financial data
yfinance>=0.2.40
arch>=7.0.0

# Progress bars
tqdm>=4.66.0

# Jupyter (optional — for interactive notebooks)
jupyter>=1.0.0
notebook>=7.0.0
jupytext>=1.16.0

# Phase 3 — QuEra Aquila (PRIMARY platform): neutral-atom analog reservoir.
# Provides the local AHS simulator (free validation) and AwsDevice for Aquila.
amazon-braket-sdk>=1.98.0

# Phase 3 — Pasqal Fresnel (2nd neutral-atom platform): Pulser + local emulator.
# pulser-simulation pulls qutip for the free local emulator; pulser-pasqal for HW.
pulser>=1.8.0
pulser-simulation>=1.8.0

# Phase 3 — Ising-machine reservoir (signed couplings: D-Wave / Fixstars Amplify).
# dwave-ocean-sdk gives the local SimulatedAnnealingSampler (free validation) + the
# D-Wave Advantage quantum annealer (Leap). amplify is the Fixstars Amplify SDK
# (GPU Ising machine, cloud; needs AMPLIFY_TOKEN). Only the SA path is needed offline.
dwave-ocean-sdk>=8.0.0
amplify>=1.6.0

# Phase 3 — gate-QPU path via qBraid (optional — preinstalled in qBraid Lab).
# Required for cloud simulator/QPU submission; local statevector-only work does not import it.
qbraid>=0.12.0

# Additional gate-QPU backend: direct IQM Resonance access to Emerald/Garnet.
# The qubit selector currently requires Qiskit <2.2.
iqm-client[qiskit]==34.0.3
iqm-qubit-selector==1.0.3
scipy-stubs==1.17.1.5

# Optional: real weather data (fallback to Lorenz if unavailable)
# meteostat>=1.6.0
